{
  "name": "vaults",
  "version": "1.0.0",
  "description": "Almost Fully Typed Vault Client for Typescript and Javascript",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "test": "VAULT_TOKEN=test jest",
    "lint": "tslint --project tsconfig.json",
    "compile": "yarn build",
    "build": "find src -name \"*types.ts\" -exec npx ts-interface-builder --ignore-index-signature {} \\; && tsc && cp package.json *.md dist",
    "cli": "node dist/cli.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "request": "^2.88.0",
    "request-promise-native": "^1.0.7",
    "ts-interface-checker": "^0.1.9"
  },
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "@types/node": "^12.7.2",
    "@types/request-promise-native": "^1.0.16",
    "jest": "^24.9.0",
    "ts-interface-builder": "^0.2.1",
    "ts-jest": "^24.0.2",
    "tslint": "^5.20.0",
    "typescript": "^3.5.3"
  }
}
